---
export interface Props {
	image: string;
	text: string;
}
const { image, text } = Astro.props;
---

<div
	class="m-auto sm:h-xl h-lg sm:w-sm w-xs rounded shadow-xl shadow-green-500 grid auto-rows-auto bg-[#343434] sm:mb-0 mb-10">
	<!-- dark:shadow-glow shadow-white-glow -->
	<div class="m-auto sm:h-15 h-20 flex">
		<span class="text-white m-auto font-extrabold text-3xl">{text}</span>
	</div>
	<img src={image} class="rounded w-full mb-0 self-end" />
</div>
